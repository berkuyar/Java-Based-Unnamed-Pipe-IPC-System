# Java-Based-Unnamed-Pipe-IPC-System
A Java-based simulation of interprocess communication using unnamed pipes. The system models producer and consumer processes interacting through a shared pipe with limited capacity. It supports blocking and wake-up mechanisms when the pipe is full or empty, and processes are scheduled based on event timings read from an input file.
